#User Input
#Static Vs Dynamic
input('Enter your name ')
#take input from users and store them in variable

fnum = int(input("Enter first number "))
snum = int(input('Eneter second number '))
#add the 2 variables
result = fnum + snum
#print the result
print(result)